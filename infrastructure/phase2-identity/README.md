# Phase 2 — Identity & Access Control

## What This Phase Builds

- User-Assigned Managed Identity (Storage → Key Vault auth)
- Azure RBAC role assignments for genomic data access
- Customer-managed key migration (completes Phase 1 encryption)
- Virtual Network with service endpoints (replaces IP firewall)

## Identity Architecture